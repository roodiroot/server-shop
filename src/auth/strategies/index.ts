import { GoogleStrategy } from "./google.strategy";
import { JwtStrategy } from "./jwt.strategy";
import { YandexStrategy } from "./yandex.startegy";

export const STRATEGIES = [
    JwtStrategy, 
    // GoogleStrategy, 
    // YandexStrategy
]